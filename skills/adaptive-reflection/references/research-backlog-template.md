# Research Backlog Template

## Backlog Partition

- Track: `project-context` | `abstract-method`
- Evidence scope: `project-only` | `cross-project` | `generic`
- Split handling: `single-track` | `paired` | `defer`
- Applicability realm hypothesis: `project-only` | `project-family` | `discipline-general`
- Applicability signal hypothesis: `user-assertion` | `field-research` | `both` | `none`
- Reason summary: one short sentence for the track choice and any split decision.
- Keep separate entries per track; do not blend contexts in one item.
- Leakage check (required for `abstract-method`): no project-specific naming/path assumptions.

## Candidate Topic

- Topic:
- Domain:
- Related skills:
- Current pain signal:
- Opportunity if solved:
- Scope (small/medium/large):
- Complexity risk:
- Promotion gate pre-check:
  - Evidence quality:
  - Stability:
  - Applicability signal:
    - User-assertion signal:
    - Field-research signal:
    - Transfer rationale in discipline terms:
  - Portability:
  - Overlap with existing skills:
  - Boundary definition:
- If this becomes a skill delta, state whether it is `update`, `new`, or `defer`.

## Research Plan

1. Baseline evidence to collect:
2. Open-source / best-practice references to review:
3. Minimal experiment:
4. Success criteria:
5. Decision rule (adopt/defer/drop):
6. Applicability decision rule:
   - Base this rule on the Promotion gate pre-check above; do not restate full gate details.
   - Keep as `project-context` when:
   - Promote to `abstract-method` when:
   - Escalate for user confirmation when:

## Output

### project-context

- Proposed project process/doc update:
- Reference material to add:
- Guardrails to enforce:

### abstract-method

- Proposed skill update/new skill:
- Reference material to add:
- Guardrails to enforce:
